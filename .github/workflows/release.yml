name: Release

on:
  push:
    branches: [main]

permissions:
  contents: write
  pull-requests: write
  id-token: write # OIDC trusted publishing to npm — no NPM_TOKEN secret required

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          fetch-depth: 0

      - uses: actions/setup-node@39370e3970a6d050c480ffad4ff0ed4d3fdee5af # v4.1.0
        with:
          node-version: 20
          cache: npm
          registry-url: 'https://registry.npmjs.org'

      - run: npm ci
      - run: npm run check
      - run: npm run build

      # npm's OIDC Trusted Publishing handshake (used below, since this
      # workflow has no NPM_TOKEN) is only implemented starting in npm
      # ~11.5.1. Node 20 bundles npm 10.8.2, which has no idea trusted
      # publishing exists -- it silently falls back to a plain anonymous
      # publish attempt, which the registry rejects with a bare 404 (not
      # 403, so it doesn't leak package existence to unauthenticated
      # callers). This is what broke the 0.1.1 release: provenance signing
      # succeeded but the actual `npm publish` PUT still 404'd. Upgrading
      # here (after `npm ci`, which needs the runner's original npm/lockfile
      # pairing) fixes it.
      - run: npm install -g npm@latest

      # If unreleased changesets exist on main, opens/updates a "Version
      # Packages" PR. If that PR was just merged (main already has the
      # version bump and no changesets left), publishes to npm instead.
      # Trusted Publishing (OIDC, configured on the npm package's Access
      # page) authenticates this step — no token, secret, or password ever
      # touches this workflow.
      - name: Create release PR or publish
        id: changesets
        uses: changesets/action@06245a4e0a36c064a573d4150030f5ec548e4fcc # v1.4.9
        with:
          publish: npm run release:publish
          version: npm run release:version
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create GitHub Release
        if: steps.changesets.outputs.published == 'true'
        uses: softprops/action-gh-release@da05d552573ad5aba039eaac05058a918a7bf631 # v2.2.1
        with:
          tag_name: veripatch@${{ fromJson(steps.changesets.outputs.publishedPackages)[0].version }}
          body: See [CHANGELOG.md](https://github.com/${{ github.repository }}/blob/main/CHANGELOG.md) for details.
          generate_release_notes: false
